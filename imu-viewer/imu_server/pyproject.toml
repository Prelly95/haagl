[project]
name = "imu-server"
version = "0.1.0"
description = "IMU WebSocket server for imu-viewer"
requires-python = ">=3.11"
dependencies = [
    "websockets>=13.0",
]

[project.optional-dependencies]
serial = ["pyserial>=3.5"]
